<ol class="breadcrumb">
  <li>Home</a></li>
  <li class="active">Manage Grades</a></li>
</ol>

<br/>

<%- partial ('partials/statusmessage.ejs') %>

<h2>Manage Grades</h2>
<button class="btn btn-success" id="addGradeButton">Add Grade</button>
<table class="table table-striped table-bordered" id="gradeTable" style="width:100%">
  <thead>
    <tr>
      <th>Grade ID</th>
      <th>Grade</th>
      <th>Admin</th>

    </tr>
  </thead>
  <tbody>
  <% if(typeof grades != "undefined" && grades.length > 0){ %>
    <% grades.forEach(function(grade){ %>
      <tr>
        <td><%= grade.grade_id %></td>
        <td><%= grade.grade %></td>
        <td class="text-center">
          <button class="btn btn-primary" id="editButton" data-gradeid="<%= grade.grade_id %>">edit</button>
          <button class="btn btn-danger" id="deleteButton" data-gradeid="<%= grade.grade_id %>">delete</button>
        </td>
      </tr>
    <% }) %>
  <% } %>
  </tbody>
</table>

<div style="display: none;" id="deleteConfirm">Are you sure you want to delete this record?</div>

<!-- this is the form to delete the student, display none means it is hidden until clicked -->
<form action="/delete_grade" method="POST" style="display: none;" id="deleteform">
  <input type="hidden" name="grade_id">
</form>

<!-- this form to add the grade is display:none so it is hidden until we click to edit grade -->
<form style="display: none;" action="" method="POST" class="form-horizontal" id="manageGradeForm">
  <input type="hidden" name="grade_id">
  <!-- <div class="form-group">
    <label for="grade_id" class="col-sm-2 control-label">Grade ID</label>
    <div class="col-sm-10">
      <input type="text" class="form-control" id="grade_id" name="grade_id" placeholder="Grade ID" required>
    </div>
  </div> -->
  <div class="form-group">
    <label for="grade" class="col-sm-2 control-label">Grade</label>
    <div class="col-sm-10">
      <input type="text" class="form-control" id="grade" name="grade"  placeholder="Grade" required>
    </div>
  </div>



</form>

<!-- we will need to create another page just like this for manage grades etc.  -->
<!-- this will connect to the manage students .js file..  -->
